{% if template.category %}
    <div class="card">
        <div class="card-block">

            <div class="media">
                <div class="media-body">

                    <h4 class="media-heading">
                        {{ template.category.name }}

                        {% if template.category.suffix.value %}
                            ({{ template.category.suffix }})
                        {% endif %}
                        <br>
                        <small class="text-muted">{{ template.category.description }}</small>
                    </h4>

                    <a href="/admin/taxes/edit/{{ template.category.id }}">{{ trans('module::message.return_to_tax') }}</a>

                </div>
            </div>

        </div>
    </div>
{% endif %}
